# Environment Variables Configuration
# Copy this file to .env and fill in your values

# Application Secret Key (REQUIRED for production)
# Generate with: openssl rand -base64 32
APPLICATION_SECRET=changeme-generate-a-secure-key

# Play Framework Environment
PLAY_ENV=prod

# HTTP Port (default: 9000)
PORT=9000
HTTP_PORT=9000

# Database Configuration (PostgreSQL for production)
# For H2 (development), leave these commented
# DB_URL=jdbc:postgresql://localhost:5432/reactive_manifesto
# DB_USER=postgres
# DB_PASSWORD=your-secure-password

# Alternative: Use DATABASE_URL (Heroku style)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Akka Configuration (optional)
# AKKA_LOGLEVEL=INFO

# Allowed Hosts (comma-separated, optional)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,localhost
